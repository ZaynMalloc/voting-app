var React = require('react');

var LoginButtons = React.createClass({

  render:function(){

    return(

      <button className="btn btn-primary" type="submit">Create A Poll</button>
      <button className="btn btn-primary" type="submit">View Created Polls</button>

    );
  }

});

var LoginButtons = React.createClass({

  render:function(){

    return(

      <button className="btn btn-primary" type="submit">Create A Poll</button>
      <button className="btn btn-primary" type="submit">View Created Polls</button>

    );
  }

});


module.exports = LoginButtons;
